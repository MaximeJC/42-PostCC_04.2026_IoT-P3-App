

apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ingress-playground
spec:
  rules:
  - host: app.local # ligne pour bonus uniquement
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: playground
            port:
              number: 8888
